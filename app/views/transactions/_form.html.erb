<%= form_with(model: transaction) do |f| %>
  <div class="form-row">
    <div class="col-sm-6">
      <%= f.select :allocation, Account::ALLOCATIONS, { include_blank: true }, { class: 'custom-select custom-select-sm' } %>
    </div>
    <div class="col-sm-6">
      <button type="submit" class="btn btn-sm btn-secondary">
        <i class="far fa-save"></i>
      </button>
    </div>
  </div>
<% end %>
